{{-- Shared by every Reports page: one summary number tile. --}}
<div class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-zinc-800 dark:bg-[#0c0c0f]">
    <p class="text-xs font-semibold uppercase tracking-wide text-zinc-500 dark:text-zinc-400">{{ $label }}</p>
    <p class="mt-2 text-2xl font-bold text-zinc-900 dark:text-zinc-100">{{ $value }}</p>
</div>
